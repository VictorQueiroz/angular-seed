'use strict';

describe('myApp.user.user-profile-controller', function() {

  beforeEach(module('myApp.user.user-profile-controller'));

  describe('user profile controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var userProfileCtrl = $controller('UserProfileCtrl');
      expect(userProfileCtrl).toBeDefined();
    }));

  });
});